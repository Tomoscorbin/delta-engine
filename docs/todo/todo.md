# Open questions and decisions

- [ ] Live-workspace verification of the warehouse backend (PR #206): one manual credentialed run of `uv run pytest -m databricks_e2e tests/e2e/test_warehouse_e2e.py --no-cov`. Verify: (1) information_schema freshness for just-created tables — the warehouse reader's existence probe reads `information_schema.tables`, so a create-then-resync flow must not see a stale absence; (2) PK/tag/property rows flow correctly through the shared `sql/rows.py` mappers (the duck-typed databricks-sql `Row` assumption; the JSON-vs-native detail-row shape risk is already handled by the shape-tolerant mappers); (3) trigger one deliberate failure and check whether `ServerOperationError` exposes a usable SQLSTATE / error condition — if it does, the follow-up is richer failure data (a `condition` field on the failure types, alongside PySpark's `getCondition()` on the Spark path), not different exception handling.
- [ ] Model struct field nullability. Gated on a richer observation source: the reader parses catalog DDL type strings, which do not carry nested NOT NULL; observing the real `StructType` (e.g. `spark.table(...).schema`) would round-trip it faithfully. Then decide whether nested-field changes stay blocked (accurately reported) or become actionable. See the type-model fidelity section of explanation-architecture.md.
- [ ] Declarable `Char(n)`/`Varchar(n)` types, if length-bounded columns become a real need. Additive on the type-model side, but flipping the read-side normalisation (observed varchar currently maps to `String`) changes drift behaviour for every existing table with such columns — needs its own migration story. See the type-model fidelity section of explanation-architecture.md.
- [ ] Live-workspace verification of the remaining hardening assumptions (from the validation-hardening series, PRs #143–#155): (1) CDF enablement really fails on a table with `_change_type`/`_commit_version`/`_commit_timestamp` columns; (2) Delta's special-character rules really apply to NESTED struct field names — the recursive declaration gate assumes they do, and over-blocks if they don't. Walkthrough step 5f already covers the third assumption (`DROP PRIMARY KEY` RESTRICT); consider 5g/6d-style walkthrough steps for these two so the manual suite keeps covering them.
- [ ] Live-workspace verification for clustering: confirm Unity Catalog's `DESCRIBE DETAIL` reports `clusteringColumns` for a liquid-clustered table (the reader depends on it; verified locally on Delta 4.x, unverified on UC). Also confirm that a partitioned→clustered conversion (blocked by `PartitioningChangeNotSupported`) is the intended behaviour rather than supporting `REPLACE PARTITIONED BY WITH CLUSTER BY` on DBR 18.1+.
- [ ] Live-workspace verification for type widening (implemented 2026-07-10, branch `feat/type-widening`): (1) per the registry admission policy, confirm a fresh table on a current Databricks Runtime does not auto-write `delta.enableTypeWidening` into DESCRIBE DETAIL properties; (2) confirm the ALTER COLUMN TYPE widening matrix on UC matches the local Delta 4.x behaviour the e2e test covers; (3) check which column roles UC refuses to widen (partition columns, clustering keys, PK/FK columns) — the engine does not model these restrictions, so they surface as execution errors per the no-runtime-preflight policy; confirm the errors are legible. Consider a 5g-style walkthrough step exercising a successful widen so the manual suite covers all three.
- [x] Renames need an identity signal beyond the name (parked 2026-07-08). RESOLVED 2026-07-12 via the `renamed_from` hint on `Column` (design: `docs/superpowers/specs/2026-07-12-column-renames-design.md`): the differ relabels the observed column through the hint and emits a `ColumnRenamed`/`RenameColumn`; the hint is inert once applied. Undeclared renames are blocked by `UndeclaredColumnRename`; ambiguous ones by `AmbiguousColumnRename`. Table renames would extend the same mechanism (`renamed_from` on `DeltaTable`), not yet built.
- [ ] Live-workspace verification for column renames (implemented 2026-07-12, branch `feat/column-renames`): (1) confirm Unity Catalog column tags and comments physically travel with `ALTER TABLE ... RENAME COLUMN` (expected yes; tag drift self-heals on the next sync regardless because tags are full-state); (2) confirm the error shapes when a CHECK constraint or generated column references the renamed column (Databricks docs say these must be changed for the rename to proceed — the engine does not model them, so they surface as execution errors per the no-preflight policy); (3) confirm that the documented PK/FK drop-on-rename does not also silently drop **other** tables' foreign keys beyond what `PrimaryKeyReferencedByForeignKeys` already blocks. Add a walkthrough step exercising a successful rename (incl. a PK-column rename) so the manual suite covers it.
- [ ] Cross-catalog inbound foreign keys are invisible to `PrimaryKeyReferencedByForeignKeys` (information_schema is per-catalog), so a cross-catalog referenced PK drop still fails at execution rather than validation. Decide whether that is acceptable long-term or worth probing the referencing catalogs (cost: catalog reads fan out). The outbound direction is rejected at declaration for the same reason (`ForeignKey` requires the referenced table to share the owner's catalog); lifting either restriction means resolving the parent side from the parent's catalog or the metastore-wide `system.information_schema`.
- [ ] Live-workspace verification (DBR 18.2+, UNIQUE constraints in Public Preview): confirm `information_schema.table_constraints` reports unique constraints with `constraint_type = 'UNIQUE'`, so the inbound-FK query's `PRIMARY KEY` filter excludes unique-backed foreign keys as intended and a PK drop is no longer blocked by them.
- [ ] Watch the inbound-FK read cost: observing referencing foreign keys adds one information_schema query per table read (PR #152). No latency evidence yet — do not optimize without it; if large syncs hurt, batch the metadata reads per catalog rather than per table.
- [ ] Release notes for the next release must call out the stricter property values: previously-accepted declarations like `"True"`, `"1_000"`, or compound intervals (`interval 1 hour 30 minutes`) now fail at declaration time with messages naming the expected format. Also call out two breaking declaration changes from the FK-pairing redesign: `primary_key` moved from a per-column `Column(primary_key=True)` flag to a `DeltaTable(primary_key=[...])` parameter, and `ForeignKey(columns={local: referenced})` replaces the positional `local_columns=[...]` list. Both old forms fail loudly at construction (unexpected keyword argument, or a missing/extra-column `ValueError`) rather than silently changing meaning. Also call out: `delta.enableTypeWidening` is now a managed key — per the registry admission policy this is breaking, since tables with the key set but undeclared start failing validation (`PropertyMustBeDeclared`) on upgrade.
- [ ] add unique columns: ALTER TABLE U ADD CONSTRAINT u_uq_email UNIQUE(email);
- [ ] Make the application layer backend-agnostic, not just backend-library-free. Today the `backend-imports-stay-in-adapters` contract only forbids importing the `delta`/`pyspark` _packages_; the application layer still hardcodes Delta-family _policy_ — the safe-change rules (`PartitioningChangeNotSupported`, `ColumnDataTypeChangeNotSupported`, `NonNullableColumnAdd`, `ColumnMappingRequiredForDrop`) exist because Databricks/Delta rejects those operations, and `application/properties.py` holds the Delta property catalogue (`DELTA_PROPERTY_REGISTRY`, `COLUMN_MAPPING_MODE_KEY`, `delta.*` key strings) consumed by `validation.py`, `api/properties.py`, and the reader. The layering is sound for now (application is the only layer all three consumers can reach without violating the `adapters|api → application → domain` order), but it means "backend-agnostic" is only half true. The real refactor: `application` holds only the _mechanism_ (how rules run, the `Rule` protocol, how `validate_diff` composes them), and the backend _specifics_ (which rules, which property keys and their restrictions) are injected from the composition root or an adapter-provided port. This must move ALL the backend-flavoured rules and the property catalogue together — moving only the property pieces just relocates a third of the problem. Larger change, own PR. See the property-ownership spec addendum for how the registry ended up in application.
- [ ] Think of whether to make DeltaTable automatically make columns unique if they are PK cols
- [ ] Allow explicit constraint names (PK/FK) on DeltaTable — both for adopting existing catalog names and as the escape hatch when two FKs' generated names collide (currently rejected at declaration with no override).
- [ ] consider replacing typle comps in validation with regular loops for readability
- [ ] Review AnalysisException catches in executor and reader
- [ ] Error-handling observability follow-ups (policy agreed 2026-07-11 during the PR #206 review): (1) log the traceback at both readers' `fetch_state` boundaries (`logger.warning(..., exc_info=True)`) before returning `ReadFailed` — the typed failure carries type + full message but not location, the boundary is the last place the stack exists, and warning-level reaches stderr/pytest/CI even with no logging config; (2) replace the warehouse executor's silent `contextlib.suppress(Exception)` on cursor close with a DEBUG log so the policy has zero silent paths; (3) write the error-handling policy into `explanation-architecture.md`: expected states are values (TableAbsent, parse→None), broad `except Exception` only at the two total port boundaries (never `BaseException`), narrow condition-checked re-raising catches everywhere else (the information_schema probe is the template), pure code never catches, no engine-level retries (transports own retry).
- [x] should report.any_failures be report.has_any_failures? RESOLVED as part of CI-grade dry runs: renamed to `has_failures`, mirroring the table-level predicate alongside the new `has_changes`.
- [ ] Decide whether to emit `RELY` on FK/PK constraints. Without `RELY`, Databricks treats informational constraints as documentation only and the optimizer cannot use them for join elimination / query rewrite. If the point of declaring keys is optimization, they are currently inert. Weigh against the risk of `RELY` on unverified data (the optimizer trusts it). Would add a `rely: bool` to `ForeignKeyConstraint`/`PrimaryKeyConstraint` and a ` NOT ENFORCED RELY` / ` NOT ENFORCED` suffix in the compiler.
- [ ] investigate TableSnapshot.**post_init**(self) on DesiredTable. Should all validation be in DesiredTable?
- [ ] Review if any classes/functions/methods/modules etc should be made private
- [ ] restructure files so that important things come first
- [ ] `ignored_properties` escape hatch for coexistence with other tooling writing managed keys (deferred from the property-ownership design; see spec Known Limitations)
- [x] Structured report output for reporting / understanding runs. DONE as part of CI-grade dry runs: `SyncReport.to_dict()` / `TableRunReport.to_dict()` project a backend-free, JSON-serialisable per-table record (name, status, has_changes/has_failures, change-summary records, full compiled SQL, failure records, statement-denominated execution counts) under `schema_version: 1`; `SyncReport.planned_sql_statements` and `has_changes` added; `TableRunReport` and the concrete failure types are now public. Still deferred: the thin `databricks.py` Spark `display()` / run-history lift — add only if interactive display or persistence becomes a real need, keeping Spark at the edge.
